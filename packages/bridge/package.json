{
  "name": "ccpocket-bridge",
  "version": "0.1.0",
  "description": "Bridge Server — WebSocket ↔ OpenClaude stdio",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "dev:bun": "bun --watch src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "start:bun": "bun src/server.ts",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "zod": "^3.22.4",
    "chokidar": "^3.6.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.4.5"
  }
}
